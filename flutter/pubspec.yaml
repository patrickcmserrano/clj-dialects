name: clojure_dialects_globe
description: Clojure dialect globe — Flutter native

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  three_js: ^0.0.29
  http: ^1.2.0
  msgpack_dart: ^2.2.0
